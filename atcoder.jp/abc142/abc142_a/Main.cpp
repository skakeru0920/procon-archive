#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)

int main() {
  int N, odd;
  cin >> N;
  odd = (N+1)/2;
  double A = odd,B = N;
  
  cout << A/N << endl;
}
